export default class FaceRegister {
    constructor(camera, userApiUrl) {
        this.camera = camera;
        this.userApi = new User(userApiUrl);
    }

    async registerFace() {
        
    }

   
}
