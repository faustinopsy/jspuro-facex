export default class Recognition {
    constructor(camera, userApiUrl) {
        this.camera = camera;
        this.userApi = new User(userApiUrl);
    }

    async recognizeFace() {
       
    }

   
}
